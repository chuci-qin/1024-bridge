# Stablenet signing configuration
# EVM_PRIVATE_KEY=               # Raw private key (hex)
# EVM_PRIVATE_KEY_PATH=          # Or: path to JSON file with .private_key field
# EVM_KEYSTORE_PATH=             # Or: path to encrypted keystore
SVM_KEYPAIR_PATH=
