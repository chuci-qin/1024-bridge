# Mainnet signing configuration (use keystore or ledger, never raw private key)
# EVM_PRIVATE_KEY_PATH=          # Path to JSON file with .private_key field
# EVM_KEYSTORE_PATH=             # Or: path to encrypted keystore
# EVM_LEDGER=true                # Or: hardware wallet (recommended)
SVM_KEYPAIR_PATH=
