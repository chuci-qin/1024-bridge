# Build artifacts
target/
node_modules/

# Foundry (EVM) - installed by forge install, built by forge build
contracts/evm/lib/
contracts/evm/out/
contracts/evm/cache/
contracts/evm/foundry.lock

# Environment files
.env
.env.*
!.env.*.example
!.env.example

# Test ledger data
test-ledger/

# Relayer runtime data
.relayer/
relayer/**/.relayer
relayer/**/.env
relayer/*.pid
relayer/logs/

# Relayer secrets
relayer/easypanel-envs/

# Keys & secrets (NEVER commit private keys)
deploy/keys/
*.pem
*.key

# Deploy config secrets & logs
deploy/config/.env
deploy/config/*/.env
deploy/config/*/deploy.log
# Safe / multisig tx payloads generated by deploy/evm/manage-roles.sh
# (calldata + signer addresses, not secrets but per-deploy ephemeral)
deploy/config/*/safe-tx/

# IDE and editor
.cursor/
.idea/
.vscode/

# Backup files
.backup_*

# Logs
*.log

# Solana keypair / wallet files
solana-wallet-*.json
faucet-keypair.json

# Legacy directories (from main branch, no longer part of dev)
/evm/
/svm/
/solana/
/relayer1/
/deploy1/
/local-debug/
/broker/
